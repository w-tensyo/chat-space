.msg
  .msg-info
    %p.msg-info__name
      = message.user.name
    %p.msg-info__text
      - if message.content.present?
        = message.content
  %p.msg-date
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")